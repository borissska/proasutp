import { CameraConfig } from "./types";

export const CAMERA_CONFIG: CameraConfig = {
  speed: 0.1,
  playerHeight: 1.8,
  playerRadius: 0.5,
  roomBounds: {
    minX: -2.8,
    maxX: 2.8,
    minY: 0,
    maxY: 4,
    minZ: -10.5,
    maxZ: 10.5,
  },
  defaultPosition: [0, 1.7, 9],
}; 